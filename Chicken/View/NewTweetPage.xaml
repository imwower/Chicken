<phone:PhoneApplicationPage 
    x:Class="Chicken.View.NewTweetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="TextBlock" BasedOn="{StaticResource TextSmall}">
                <Setter Property="TextAlignment" Value="Left" />
            </Style>
        </Grid.Resources>
        <!--title-->
        <StackPanel x:Name="TitlePanel"
                    Grid.Row="0" Margin="10">
            <TextBlock x:Name="ApplicationTitle"
                       Text="what's happening?" />
        </StackPanel>
        <Grid x:Name="MainPanel"
              Grid.Row="1" Margin="25,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <!--content-->
            <TextBox Text="Hello World" Grid.Row="0"
                     TextWrapping="Wrap" />
            <StackPanel x:Name="MenuPanel" Grid.Row="1"
                        Orientation="Horizontal" Opacity="0.6">
                <!--char counter-->
                <TextBlock Text="140" />
                <TextBlock Text=" image" />
                <TextBlock Text=" location" />
            </StackPanel>
        </Grid>
    </Grid>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <!--tweet-->
            <shell:ApplicationBarIconButton IconUri="/Images/dark/appbar.send.png" Text="tweet"/>
            <!--add image-->
            <shell:ApplicationBarIconButton IconUri="/Images/dark/appbar.camera.png" Text="image"/>
            <!--at somebody-->
            <shell:ApplicationBarIconButton IconUri="/Images/dark/appbar.mention.png" Text="image"/>
            <!--emotion-->
            <shell:ApplicationBarIconButton IconUri="/Images/dark/appbar.emotion.png" Text="image"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="菜单项 1"/>
                <shell:ApplicationBarMenuItem Text="菜单项 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
